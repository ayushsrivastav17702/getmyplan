# DATA UPLOAD INFRASTRUCTURE, MASTER DATA & TRANSACTIONAL DATA — FULL AUDIT RESPONSE

**Date**: April 10, 2026 | **Application**: GetMyPlan | **Auditor**: Codebase Analysis

---

## PART 1: DATA UPLOAD INFRASTRUCTURE

### 1.1 File Upload Capabilities

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 1.1.1 | What file formats are currently supported for data upload? | **CSV + Excel** | `_read_file()` in `upload_service.py` L138-151 supports: `.csv` (with multi-encoding fallback: UTF-8, UTF-8-BOM, Latin-1, CP1252) and `.xlsx`/`.xls` (via `pd.read_excel`). No JSON or Parquet support. | No JSON, Parquet, or TSV support. No file format auto-detection beyond extension. | 2 formats |
| 1.1.2 | What is the maximum file size supported for single upload? | **50 MB** | `MAX_FILE_SIZE_BYTES = 50 * 1024 * 1024` in `upload.py` L25. Returns error code `E049` if exceeded. File is fully read into memory via `await file.read()` before size check (L347). | 50MB hard limit set in code. Kubernetes ingress proxy may impose additional limits. Entire file is loaded into memory before validation. | 50 MB |
| 1.1.3 | What is the maximum rows per file currently supported? | **No explicit row limit** | No row count validation exists. Practical limit is determined by 50MB file size + Pandas in-memory processing. CSV at ~100 bytes/row = ~500K rows per 50MB file. Validation engine processes all rows. | Memory-bound. For large files, Pandas loads entire DataFrame into container RAM (~512MB-1GB available). Estimated practical ceiling ~500K-1M rows depending on column count. | ~500K rows |
| 1.1.4 | Do you support parallel uploads (multiple files simultaneously)? | **Not explicitly** | Each upload endpoint is an independent async FastAPI handler (`upload.py` L48-103). Multiple browser tabs or API calls can upload concurrently since FastAPI handles async requests. However, there's no coordinated multi-file upload UI or batch upload API. | No multi-file dropzone in frontend. No upload grouping or batch session concept. No progress tracking for parallel uploads. Race conditions possible on `replace_existing=True` deletes. | Single-file per request |
| 1.1.5 | Do you support chunked uploads for large files (>500MB)? | **Not implemented** | File is read in full via `await file.read()` (L347). No chunked/resumable upload protocol (e.g., tus.io). The 50MB hard limit prevents >500MB files entirely. | No resumable uploads. Connection drop = full re-upload. Not possible with current 50MB limit. | Not available |
| 1.1.6 | Do you support async upload processing with job queues? | **Not implemented** | Upload is fully synchronous: read file → validate → save to MongoDB → return response, all within one HTTP request/response cycle (L337-443). `BackgroundTasks` parameter exists in handler signatures but is unused. | Large files block the request until complete. No Celery, RQ, or any task queue. No webhook/polling for long-running uploads. | Synchronous only |
| 1.1.7 | What compression formats are supported? (ZIP, GZIP) | **Not supported** | No decompression logic. Only raw CSV and Excel files accepted. Compressed files would fail at `pd.read_csv()` or `pd.read_excel()`. | Users must decompress before upload. No `.zip`, `.gz`, `.tar`, or `.7z` support. | None |

---

### 1.2 Data Validation Layer

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 1.2.1 | Do you have a schema registry for validating file structures? | **Implemented (Code-based)** | `REQUIRED_COLUMNS` dict in `upload_service.py` L20-31 defines required columns per upload type (8 upload types). `DEDUP_KEYS` dict (L33-41) defines uniqueness constraints. This is a code-level schema registry, not an external service. | Schema is hardcoded in Python — not in a DB or external registry. Adding new upload types requires code changes. No versioned schemas. | 8 schemas |
| 1.2.2 | What validation rules are currently implemented? | **75 rules implemented** | Full error catalog in `utils/upload_errors.py` (150 error code entries). Rules include: **Column presence** (E043 missing columns, E044 extra columns), **Data types** (E001 numeric SKU, E010 scientific notation, E019 invalid dates), **Null checks** (E045 empty file, E005 empty SKU), **Range validation** (E032 unusually large quantities, E040 unusually large revenue, E068 negative inventory), **Uniqueness** (E004 duplicate master key, E050 duplicate rows), **Cross-reference** (E003 SKU not in master, E011 store not in master), **Business rules** (E066 low stock, E067 available > on-hand, E070 test data detection), **Currency** (mixed currency detection, E036 currency conversion), **Date** (E020 future dates, E023 text dates like "today"/"yesterday"), **File-level** (E049 file too large, E054 duplicate file hash). | Some rules are warnings-only (non-blocking). No custom rule definitions per tenant. Validation throughput not benchmarked at scale. | 75 rules |
| 1.2.3 | Do you support custom validation rules per client? | **Not implemented** | All validation rules are global, defined in `UniversalUploadService`. No per-tenant rule configuration. The Data Quality Rules Engine (`/api/data-quality-rules`) exists as a separate module with 6 rule types (threshold, range, regex, null_check, uniqueness, cross_reference) but it's not integrated into the upload validation pipeline. | Custom rules exist in a separate module but are not wired into upload-time validation. Would need integration work. | Global rules only |
| 1.2.4 | Is there real-time validation during upload or batch after? | **Batch validation** | Entire file is read into a Pandas DataFrame, then all validations run sequentially in `process_file()` (L55-135). There's also a **validate-only endpoint** (`POST /api/upload/v2/{type}/validate`) that runs validation without saving (L109-115). | No streaming/row-by-row validation during upload. Entire file must be processed before results are shown. | Batch |
| 1.2.5 | How are validation errors reported back to user? | **Inline + Summary** | Response JSON includes: `errors[]` (blocking errors with code, category, message, user_message, severity), `warnings[]` (non-blocking), `corrections[]` (auto-fixes applied), `approval_items[]` (unusual values needing confirmation), `preview[]` (first 5 rows), `total_rows`, `valid_rows`. Frontend renders these inline in the FileDropzone component with severity color coding. | No downloadable error file (CSV/Excel with row-level errors). No per-row error annotation. Errors reference counts but not specific row numbers (except `approval_items` which include row index). | JSON response |
| 1.2.6 | Do you support partial upload acceptance? | **No** | If any blocking error exists (`severity: "blocking"`), the entire upload fails (`result["success"] = False`, L106). Warnings and corrections do not prevent saving. Bad rows are tagged with `_has_error = True` but the whole file is rejected if any blocking error occurs. | All-or-nothing for blocking errors. No option to skip bad rows and save good ones. Corrections (auto-fixes) are applied to all rows before the blocking check. | All-or-nothing |
| 1.2.7 | What is the validation throughput (rows/sec) at scale? | **Not benchmarked** | Pandas-based validation on single thread. Current seeded data: ~30K sales rows validates in <2s. Cross-reference validation (E003, E011) involves set lookups which are O(n). Duplicate detection uses `DataFrame.duplicated()`. | No formal throughput benchmark. Estimated ~50K-100K rows/sec for simple validations, slower for cross-reference checks against large master datasets. | Estimated ~50K-100K rows/sec |

---

### 1.3 Data Storage & Partitioning

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 1.3.1 | What is the database/warehouse used for uploaded data? | **MongoDB** | Motor async driver. V2 collections store one document per row (`daily_sales`, `store_inventory`, `warehouse_inventory`, `sku_master`, `store_master`, `warehouse_master`). V1 legacy stores entire CSV as a JSON array inside one document in `uploaded_files` collection. | No dedicated data warehouse (no BigQuery, Snowflake, Redshift). MongoDB serves both transactional and analytical workloads. Document store, not columnar. | MongoDB |
| 1.3.2 | How are uploaded tables partitioned? | **By tenant (DB-level)** | Each tenant gets a separate MongoDB database (`tenant_{tenant_id}`). Within a tenant DB, V2 collections are not partitioned — all data for all dates/stores in one collection per type. Compound indexes on `(tenant_id, day)` and `(tenant_id, sku)` provide query performance. | No date partitioning, no store/region partitioning within a collection. No sharding configured. All historical data in a single collection per type. | DB-per-tenant |
| 1.3.3 | Do you use columnar storage for analytical queries? | **No** | MongoDB document storage (BSON). All analytical queries load documents into Pandas DataFrames in-memory via `get_cached_data()` which fetches up to 200,000 documents per call (L417). No columnar format. | No Parquet, Arrow, or columnar optimization. Full document scans for analytics. No column projection beyond `_id/tenant_id/uploaded_at` exclusion. | Document store |
| 1.3.4 | What is the data retention policy for raw uploads? | **No automated retention** | All uploaded data persists indefinitely in MongoDB. No TTL indexes on data collections. Upload history (`upload_history` collection) also persists indefinitely. Only the shared DB has TTL on `verification_token_expiry` (48h). | No auto-archival, no data lifecycle management. Storage grows unbounded. No cold storage tier. | Unlimited retention |
| 1.3.5 | Do you maintain version history of uploaded files? | **Partial** | `upload_history` collection records every upload session with: `file_name`, `file_hash` (SHA256), `rows_uploaded`, `status`, `uploaded_by`, `uploaded_at`, `session_id`. Accessible via `GET /api/upload/v2/history` and `GET /api/upload/v2/history/days`. Duplicate file detection via hash (E054). | History tracks metadata only — not the actual data content of each upload. Cannot roll back to a previous version of data. `replace_existing=True` deletes old data before inserting new. | Metadata history only |
| 1.3.6 | How do you handle incremental vs full uploads? | **Configurable per upload** | Daily transactional uploads (`daily_sales`, `store_inventory`, `warehouse_inventory`) accept `replace_existing` param. When `true`: daily_sales deletes by day, inventory deletes all, masters delete all and re-insert (L527-539). When `false`: appends new records. Master uploads always use `replace_existing=True` (L84, 93, 102). | No merge/upsert logic — it's either append or delete+re-insert. No CDC (change data capture). No field-level update for existing records. No partial day updates for daily_sales. | Append or Replace |
| 1.3.7 | What indexing strategy exists on upload tables for fast lookups? | **Compound indexes** | Created at startup in `_ensure_v2_collection_indexes()` (server.py L3325-3344): `daily_sales: (tenant_id, day)`, `(tenant_id, sku)`. `store_inventory: (tenant_id, store_code)`, `(tenant_id, ean)`. `warehouse_inventory: (tenant_id, sku)`. `sku_master: (tenant_id, sku) UNIQUE`. `store_master: (tenant_id, store_code) UNIQUE`. `warehouse_master: (tenant_id, warehouse) UNIQUE`. `demand_plans: (category, status)`, `created_at`. `uploaded_files: file_type`. | Indexes cover primary lookups but not all query patterns. No index on `daily_sales.store_code` or `store_inventory.day` individually. No text indexes for search. No index on revenue for top-N queries. | 11 indexes |

---

## PART 2: MASTER DATA MANAGEMENT

### 2.1 Store Master

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 2.1.1 | Current number of stores supported per client? | **No hard limit** | V2 `store_master` collection with no document count limit. Current seed data: 5 stores (MAIN-01, SOUTH-02, WEST-03, ONLINE-01, POPUP-01). All algorithms iterate over stores in-memory via Pandas. | Practical limit bounded by Pandas memory. At 10,000 stores with 10,000 SKUs, store_inventory = 100M rows — would require architectural changes. | 5 (seeded), unlimited (schema) |
| 2.1.2 | How are store hierarchies stored? | **Flat attributes** | Store master fields: `store_code`, `store_name`, `channel` (Retail/Online), `city`, `region` (West/South/North), `class` (A/B/C), `cluster` (Metro/Tier1/Online), `area`, `is_online`, `open_date`, `etl_new`. See seed data L49-53. | No true hierarchical tree (Region → Zone → Cluster → Store). Flat denormalized attributes. No hierarchy management API. Filtering uses individual fields. | 6 hierarchy-like attributes |
| 2.1.3 | Do you support store attributes? | **Implemented** | Size (`area`: "500sqft"), Grade/Class (`class`: A/B/C), Tier/Cluster (`cluster`: Metro/Tier1/Online), Importance (`class` used for priority weighting in replenishment — A>B>C at L532-577 in replenishment.py). Region, City, Channel, Open Date, ETL New flag. | No custom numeric weights per store. Class A/B/C is a proxy for importance. No configurable store scoring model. | 10+ attributes |
| 2.1.4 | How frequently is store master updated? | **On-demand (manual upload)** | Updated via `POST /api/upload/v2/store-master` which always uses `replace_existing=True` (L93). No scheduled refresh. No API for individual store CRUD. | No real-time sync with POS/ERP. No API to add/update a single store. Full replacement on each upload. | Manual upload |
| 2.1.5 | Do you support historical store changes? | **Not implemented** | Store master is a point-in-time snapshot. Each upload replaces the previous data. No store change log, no effective dates, no history of relocations/closures/status changes. | No temporal store tracking. Cannot query "what stores existed on date X". No audit trail for store changes (beyond upload_history metadata). | Not available |
| 2.1.6 | How are store enable/disable changes propagated to algorithms? | **Not automated** | No `is_active` or `enabled` field on store documents. All stores in store_master are considered active by all algorithms. To "disable" a store, it must be excluded from the next store_master upload. Onboarding stores have `is_active` flag, but V2 store_master does not. | No soft-delete or enable/disable toggle. Removing a store from master breaks historical sales references for that store. | Not available |

---

### 2.2 Product & SKU Master

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 2.2.1 | Current number of SKUs supported per client? | **No hard limit** | V2 `sku_master` collection with unique index on `(tenant_id, sku)`. Current seed: 10 SKUs. All algorithms load full SKU list into Pandas DataFrame. | Practical limit ~100K SKUs before memory becomes a concern (each SKU doc ~500 bytes = 50MB for 100K). Cross-join with stores amplifies memory usage. | 10 (seeded), unlimited (schema) |
| 2.2.2 | Current number of styles supported per client? | **No hard limit** | V1 `style_master` in `uploaded_files` collection. Current seed: 8 styles (mapped to 10 SKUs). V2 has no dedicated `style_master` collection — `_V2_MAP` maps `style_master` to `None` (server.py L394), so it falls back to V1. | Style master remains in V1 only. Not yet migrated to V2 individual-document format. Adding V2 style_master would require schema update. | 8 (seeded) |
| 2.2.3 | How many hierarchy levels are supported? | **5 levels** | Style master supports: `master_category` (Apparel/Accessories/Footwear) → `category` (Tops/Bottoms/Accessories/Footwear) → `subcategory` (T-Shirts/Hoodies/Joggers/Sneakers/etc.) → `style_code` (TSHIRT-BLK) → `ean`/SKU (TSHIRT-BLK-M). Plus `gender`, `brand`, `season`, `segment`, `family`, `fashion_grade`. | Department = master_category. No formal hierarchy tree management — flat denormalized fields. Cannot add hierarchy levels without schema change. | 5 levels |
| 2.2.4 | Do you support custom attributes? | **Implemented** | 9 custom attribute fields: `attribute1` through `attribute9` on style master. Attribute grouping module (`core_logic.py` L579) processes them for analysis. Also: `segment`, `fashion_grade`, `family`, `vendor`, `description`, `image_url`. | Fixed 9 attribute slots, not dynamic. Cannot rename attributes per tenant. No attribute data types (all treated as strings). | 9 custom + 6 standard |
| 2.2.5 | How is size normalization handled? | **Not implemented** | Size stored as-is from upload: "M", "L", "ONE", "3PK", "9", "500". Healthy Size Set analysis in `core_logic.py` L279 detects broken size curves but uses raw values. No `S=1, M=2, L=3, XL=4` normalization. | Cannot compare sizes across brands (Nike M vs Zara M). No size ordering or size range definition. Size gap analysis works on raw strings. | Raw strings |
| 2.2.6 | Do you support product lifecycle states? | **Partial** | `season` field (SS25, AW25) and `etl_new` flag on stores. No explicit product lifecycle state machine (New → Active → Discontinued → Seasonal). `fashion_grade` (A/B/C) provides quality tier. | No lifecycle state field. Cannot mark a SKU as "Discontinued" or "End of Life". No automatic state transitions based on sales data. | Season + Grade only |
| 2.2.7 | How are seasonal products managed? | **Via `season` field** | Season field on style_master (e.g., "SS25", "AW25"). Filter endpoints accept season parameter. No automatic season-based business rules (e.g., auto-markdown for end-of-season). | Season is a static label. No season calendar, no automatic transition, no season-aware DOH or replenishment rules. | Label only |
| 2.2.8 | Do you support product substitutes/alternatives? | **Not implemented** | No substitute/alternate style mapping. Replenishment module has no "Replacement" feature. No similarity matrix or category-based substitution. | Would need a new `product_substitutes` collection and integration into replenishment/stockout modules. | Not available |
| 2.2.9 | What is the SKU enrichment latency from upload to algorithms? | **~0 seconds (synchronous)** | Upload saves to `sku_master` collection (L504-542 in upload.py). Next API call to any algorithm reads from the same collection via `get_cached_data()`. No async pipeline or cache invalidation delay. | Immediate availability. No warm-up or cache refresh needed. However, `get_cached_data()` reads full collection every time (no row-level invalidation). | Instant |

---

### 2.3 Planogram Management

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 2.3.1 | Is planogram data store-specific or cluster-based? | **Store-specific** | Planogram fill rate calculated per `store_code × ean` combination in `planogram.py` L78-160. `norm_allocated` derived from historical max stock per store×sku (L99). No cluster-based planogram sharing. | Cannot apply one planogram template to a cluster of stores. Each store's "norm" is independently derived from its own historical maximum. | Per store×SKU |
| 2.3.2 | How many planogram entries per store typically? | **Stores × SKUs** | Entries = number of unique `(store_code, ean)` combinations with historical inventory data. Current: 5 stores × 10 SKUs = 50 entries. No explicit planogram upload — norm is auto-derived from max inventory levels. | No dedicated planogram upload or management UI. "Planogram" is synthesized from inventory history, not defined by merchandisers. | 50 (current) |
| 2.3.3 | Do you support seasonal planogram changes? | **Not implemented** | Norm is calculated from all-time max stock. No seasonal planogram rotation. No mechanism to define different norms for SS25 vs AW25. | Static norm derived from historical peak. Summer and winter planograms cannot coexist. | Not available |
| 2.3.4 | How are facing allocations (norm_allocated) stored and updated? | **Auto-derived, not stored** | `norm_allocated = max(quantity) per (store_code, ean)` from inventory history (`planogram.py` L99). Recalculated on every API call. Not persisted to database. | No manual facing allocation. Cannot set norm_allocated = 20 for a specific store×SKU. No planogram upload endpoint. | Computed per request |
| 2.3.5 | Do you support replenishment cycle days per category/style? | **Not category-specific** | `cover_days` parameter on replenishment endpoint is global (not per-category). Default = `lead_time_days + safety_days` from tenant config. Per-SKU `lead_time_days` exists in SKU master. | No per-category cover days. Per-SKU lead time partially covers this via the reorder endpoint in ai_demand module. | Global param |
| 2.3.6 | Do you support cover days configuration per category? | **Via tenant config** | `category_ideal_doh` config map in DOH analysis (L105-106 in doh_analysis.py) allows per-category DOH targets. However, replenishment module uses a single `cover_days` parameter. | DOH and replenishment configs are separate. DOH has per-category config, replenishment does not. No unified cover days per category for replenishment. | DOH only |
| 2.3.7 | Do you support top seller multiplier for additional cover? | **Implemented** | DOH module: `ideal_doh × topseller_multiplier` (default 2.0×, configurable). `doh_analysis.py` L92,112. Topseller classification via X-Factor in AI Demand module. | Multiplier is global (not per-category). Binary topseller/non-topseller (no graduated tiers). | 2.0× default |
| 2.3.8 | Is there a planogram versioning system? | **Not implemented** | No planogram version tracking. Norm is recalculated from current inventory data on each request. No history of planogram changes. | Cannot compare "Jan planogram" vs "Feb planogram". No change tracking. | Not available |
| 2.3.9 | How are planogram breaches detected? | **Implemented** | Fill rate classification: `Green ≥90%`, `Yellow 80-90%`, `Red <80%` (`_classify()` L70-75). Missing facings = `norm_allocated - current_stock` (L109). Lost sales = `missing_facings × ROS × ASP` (L137). Results include per-store and per-category fill rates. | No alerts/notifications on breach. No topseller-specific breach priority. Detection is on-demand (API call), not automated. | 3-tier classification |

---

## PART 3: TRANSACTIONAL DATA

### 3.1 Sales Transactions

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 3.1.1 | What is the daily sales volume (rows/day) per client? | **~41 rows/day (seeded)** | Seed data: 10 SKUs × 5 stores × ~82% daily sale probability = ~41 rows/day. Over 757 days = 30,961 total rows. Real clients would vary: 1,000 SKUs × 100 stores = potentially 100K rows/day. | Current data is small-scale demo. System not tested at 100K+ rows/day. Upload and validation would take longer at scale. | ~41/day (seeded) |
| 3.1.2 | What is the historical sales data depth stored? | **25 months (seeded)** | Apr 2024 → Apr 2026 (757 days). `seed_historical_data.py` L21. V1 stores all as one document. V2 stores one document per row. No auto-archival or purging of old data. | All historical data kept in active collection. No hot/cold tier. At 100K rows/day × 2 years = 73M rows — would exceed memory. | 25 months |
| 3.1.3 | How is sales data ingested? | **Batch CSV upload** | Via `POST /api/upload/v2/daily-sales` with CSV/Excel file. 75-rule validation before save. Also seeded via Python script for demo data. | No streaming API, no real-time POS integration, no SFTP auto-ingest (SFTP module exists for monitoring but not for auto-upload). No API for single-transaction insert. | Batch upload |
| 3.1.4 | What is the latency from POS to availability in dashboard? | **Manual upload latency** | After CSV upload completes (~2-5s for validation + save), data is immediately available in next dashboard API call. No pipeline delay. | POS → CSV export is manual. The "latency" is human-dependent (could be same-day or next-day depending on client workflow). System latency is <5s. | ~2-5s (system) |
| 3.1.5 | Do you capture returns separately or as negative sales? | **Not explicitly handled** | Sales schema: `sku, store_code, day, quantity, revenue`. Negative quantities generate a warning (`E027` in upload_service.py L348-361) but are not blocked (except for `closing_stock` which blocks negatives via `E068`). Returns are not a separate data type. | No dedicated returns table. No return reason codes. No return-to-original-transaction linking. Negative quantity rows could represent returns but are just warnings. | Warning on negative |
| 3.1.6 | Do you store promotion/discount details with each transaction? | **Partial** | `discount_value` field exists in seeded sales data (L116 seed script). `revenue` field captures net revenue after discount. No promotion ID, campaign name, or discount reason field. Upload schema requires only: `sku, store_code, day, quantity, revenue` (no discount column in required schema). | Discount value exists in seeded data but is not a required upload field. No promotion master table. No ability to tag sales as "promotional" vs "full price". | Revenue includes discount |
| 3.1.7 | How do you handle multi-channel sales? | **Via `channel` field** | Sales have `channel` field (seeded: "online"/"offline"). Store master has `channel` (Retail/Online) and `is_online` flag. All analytics endpoints support channel filtering via query parameter. Revenue-by-channel aggregation in `TenantDataProvider.get_revenue_by_channel()`. | Channel is store-derived (not per-transaction). A sale at MAIN-01 is always "offline". No marketplace breakdown (Amazon/Flipkart). No app-specific channel. | Store-based channel |
| 3.1.8 | What is the average query response time for sales aggregation? | **~1-3 seconds** | `get_cached_data('daily_sales')` fetches up to 200K documents from MongoDB into Pandas DataFrame. Aggregation (groupby, mean, sum) runs in-memory. For 30K rows: ~1s. For 200K rows: ~2-3s. | Response time scales linearly with data size. No pre-aggregation. 1M+ rows would take 5-10s+. No query result caching. | 1-3s (30K rows) |
| 3.1.9 | Do you pre-aggregate sales at daily store-SKU level? | **Not pre-aggregated** | Raw transaction rows stored as-is. Every API call reads full collection and aggregates on-the-fly via Pandas `groupby()`. `get_cached_data()` returns raw DataFrame every time. | No materialized views. No daily_store_sku_summary table. Same aggregation recomputed on every request. CPU wasteful at scale. | On-the-fly only |

---

### 3.2 Net Sales (COGS)

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 3.2.1 | Is COGS data uploaded separately or derived from sales? | **Not implemented** | No COGS/cost field in any schema. Revenue represents selling price × quantity. No cost price, purchase price, or COGS upload type. `mrp` field in SKU master is the maximum retail price, not cost price. | Cannot calculate gross margin, contribution margin, or profitability. Revenue-only analytics. | Not available |
| 3.2.2 | What is the granularity of COGS? | **N/A** | No COGS data exists at any granularity. | — | Not available |
| 3.2.3 | How are margin calculations currently performed? | **Not implemented** | No margin calculation anywhere in the codebase. Lost sales calculations use `ROS × ASP` (selling price), not margin-based. Revenue-based analytics only. | Cannot differentiate between high-revenue-low-margin and low-revenue-high-margin SKUs for prioritization. | Not available |
| 3.2.4 | Do you support different COGS for different channels/stores? | **N/A** | No COGS data at any level. | — | Not available |

---

### 3.3 Inventory Snapshot (SOH)

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 3.3.1 | How often is inventory snapshot captured? | **Daily (via upload)** | Store inventory uploaded daily via `POST /api/upload/v2/store-inventory`. Seeded data has one snapshot per day per store×SKU for 757 days. Upload validation runs on each snapshot file. | Snapshot frequency depends on client upload cadence. No real-time inventory feed. System supports any frequency (can process multiple snapshots per day). | Daily (expected) |
| 3.3.2 | What is the inventory table size per client? | **stores × SKUs × days** | Seeded: 5 stores × 10 SKUs × 757 days = 37,850 rows. For real client: 100 stores × 10,000 SKUs × 365 days = 365M rows/year — far exceeding current architecture. V2 collection stores one document per row. | Storage grows linearly with days retained. No retention policy. At scale, would need date-based partitioning or archival. | 37,850 (seeded) |
| 3.3.3 | Do you store warehouse inventory separately? | **Yes** | Separate V2 collection `warehouse_inventory` with fields: `sku, warehouse, on_hand_qty, available_qty, allocated_qty` (auto-calculated). Separate from `store_inventory`. Warehouse module has dedicated endpoints (`/analytics/warehouse/*`). | Two separate collections means no unified "total inventory" view without cross-collection join (done in Pandas). | Separate collections |
| 3.3.4 | How do you handle in-transit inventory? | **Implemented** | Warehouse module: `GET /analytics/warehouse/transfers/in-transit` (L373-380 in warehouse.py) tracks transfers with status `in_transit`. Transfer lifecycle: pending → allocated → approved → in_transit → delivered. `total_in_transit` aggregated across all active transfers. | In-transit tracked only for warehouse-to-store transfers (created via API). Not connected to vendor shipments or POs. Not deducted from warehouse stock automatically. | Transfer-based |
| 3.3.5 | Do you support inventory reconciliation? | **Implemented** | Warehouse reconciliation endpoints: `GET/POST /analytics/warehouse/reconciliation` (L220-248). Records system_qty vs physical_qty with variance calculation. Adjustment log: `GET/POST /analytics/warehouse/adjustments` (L251-280) with reason codes. | Reconciliation is manual recording — does not auto-correct inventory. No automated cycle count scheduling. Warehouse-level only (no store-level reconciliation). | Manual recording |
| 3.3.6 | How is stock adjustment captured? | **Implemented** | `POST /analytics/warehouse/adjustments` (L264-280): records `previous_qty`, `new_qty`, `change`, `reason`, `adjusted_by`, `adjusted_at`. Stored in `warehouse_adjustments` collection. | Adjustment reasons are free-text, not from a controlled list (damage, theft, cycle count). No auto-sync with inventory collection. Recording only, does not update actual stock. | Audit log only |
| 3.3.7 | What is the snapshot date field format and timezone handling? | **ISO 8601 / UTC** | Date field: `day` in format `YYYY-MM-DD` (string). All server-side timestamps use `datetime.now(timezone.utc)`. Upload validation converts dates via `pd.to_datetime()` with `errors='coerce'`. Frontend displays dates without timezone conversion. | Stored as string `YYYY-MM-DD`, not MongoDB ISODate. No timezone per store (all UTC). Future dates generate warning (E020) but are not blocked. | UTC strings |

---

### 3.4 Open Orders & Supply Pipeline

| # | Question | Status | Details | Limitations | Scale |
|---|----------|--------|---------|-------------|-------|
| 3.4.1 | Do you track open POs from vendors to warehouse? | **Not implemented** | No Purchase Order (PO) data model. Reorder recommendations (ai_demand reorder endpoint) calculate suggested order quantities but do not create POs. No vendor master table. | Cannot track vendor delivery pipeline. Reorder suggestions are informational only — no actionable PO creation flow. | Not available |
| 3.4.2 | Do you track in-transit stock from warehouse to stores? | **Implemented** | Warehouse transfer lifecycle with `in_transit` status. `GET /analytics/warehouse/transfers/in-transit` returns all active transfers with `total_in_transit` qty. Transfer goes through: pending → allocated → approved → in_transit → delivered. | Only tracks warehouse-to-store transfers created within the system. No vendor-to-warehouse in-transit. No ETA calculation. | WH→Store only |
| 3.4.3 | What vendor lead times are captured? | **Per-SKU static** | `lead_time_days` field on SKU master (seed: TSHIRT=7d, HOODIE=14d, SNEAKER=21d, SOCKS=3d, etc.). Used in reorder point and safety stock calculations. Fallback: 14 days (configurable via query param). | Static per-SKU, not per-vendor. No historical lead time tracking or dynamic adjustment based on actual delivery performance. No vendor master. | Static per SKU |
| 3.4.4 | Do you support MOQ tracking per vendor/style? | **Partial** | Replenishment module accepts `moq` query parameter (REP-11, L381, L474-479). Order quantity rounded up to MOQ minimum. Also `pack_size` parameter for pack multiple rounding (REP-12, L482-489). | MOQ is a global parameter per API call, not per-vendor or per-SKU. No MOQ stored in master data. User must specify MOQ each time. | Global parameter |
| 3.4.5 | How are supplier constraints stored? | **Not implemented** | No supplier/vendor master table. No capacity constraints, minimum order values, payment terms, or supplier lead time variability tracking. Lead time is on SKU master, not vendor. | Would need: vendor_master collection, vendor×SKU mapping, capacity/MOQ/lead time per vendor. | Not available |
| 3.4.6 | Do you track order statuses? | **Warehouse transfers only** | Transfer status lifecycle: `pending` → `allocated` → `approved` → `in_transit` → `delivered`. Status transitions via dedicated PUT endpoints (L329-370 in warehouse.py). No PO status tracking. | Status tracking exists only for internal warehouse-to-store transfers. No vendor PO lifecycle (Open → Confirmed → Shipped → Delivered). | Transfer status only |
| 3.4.7 | How are expected delivery dates updated when delays occur? | **Not implemented** | No expected delivery date field on transfers or orders. No delay tracking or ETA adjustment mechanism. Transfer has `dispatched_at` and `received_at` timestamps but no `expected_at`. | Cannot forecast warehouse transfer arrival. No SLA monitoring or delay alerting. | Not available |

---

## SUMMARY SCORECARD

| Area | Score | Key Strength | Key Gap |
|------|-------|-------------|---------|
| **File Upload (1.1)** | 5/10 | 75-rule validation, multi-encoding CSV support | No chunked upload, no async processing, 50MB limit, no compression |
| **Validation (1.2)** | 8/10 | 75 rules, auto-fix, cross-reference, currency detection | No custom rules per tenant, no partial acceptance, no downloadable error file |
| **Storage (1.3)** | 5/10 | V2 per-row storage, V1 fallback bridge, compound indexes | No columnar storage, no partitioning, no retention policy, no caching |
| **Store Master (2.1)** | 5/10 | Flat hierarchy with 6 attributes, class-based prioritization | No hierarchy tree, no historical changes, no enable/disable, no CRUD API |
| **SKU Master (2.2)** | 6/10 | 5-level hierarchy, 9 custom attributes, per-SKU lead time | No size normalization, no lifecycle states, no substitutes, style master V1 only |
| **Planogram (2.3)** | 4/10 | Auto-derived norm, fill rate, lost sales, 3-tier classification | No planogram upload, no seasonal rotation, no versioning, norm not manually settable |
| **Sales (3.1)** | 5/10 | Daily upload, 75-rule validation, channel filtering | No streaming ingest, no POS integration, no returns handling, no pre-aggregation |
| **COGS (3.2)** | 0/10 | — | No COGS data, no margin calculations, no profitability analysis |
| **Inventory (3.3)** | 7/10 | Separate store/WH, reconciliation, adjustments, in-transit tracking | No real-time feed, reconciliation is manual, adjustments don't auto-update stock |
| **Supply Pipeline (3.4)** | 2/10 | WH→Store transfer lifecycle with 5 statuses | No PO tracking, no vendor master, no ETA, no supplier constraints |
